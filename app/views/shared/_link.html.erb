
<%= form_for [linkable, link] do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= f.text_field :url %>
  <%= f.submit "Add URL", class: "btn btn-large btn-primary" %>
<% end %>
